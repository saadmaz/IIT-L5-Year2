<?xml version="1.0" encoding="UTF-8"?>
<Context path="/bookstore-api">
    <!-- No resource definitions needed since we're using in-memory storage -->
    <!-- This is a minimal context configuration for a JAX-RS application -->
    <WatchedResource>WEB-INF/web.xml</WatchedResource>
    <Parameter name="applicationName" value="Bookstore RESTful API" override="false"/>
</Context>